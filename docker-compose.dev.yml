# An overlay for local development: builds the image from source instead of
# pulling it from the registry.
#
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
#
# This file is not needed on the server.
services:
  hltv-notify:
    build: .
    image: hltv-notificator:local
